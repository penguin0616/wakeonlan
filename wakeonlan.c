#include <stdio.h>
#include <string.h>

void createMagicPacket(char mac_address) {
	char payload[106];
	
	memset(&payload, 0, sizeof(payload));
}

int main() {
	printf("Hello, world!\n");
	return 0;
}